server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/canchago?useSSL=false&serverTimezone=UTC
    username: root
    password: Arkhan857*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB

# Carpeta donde se guardan las imágenes
canchago:
  storage:
    location: uploads

